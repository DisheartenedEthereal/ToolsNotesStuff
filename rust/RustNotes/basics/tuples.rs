fn main() {
    let tup = (500, 6.4, 1);
    // very simple, list, u assign variables to it, they can have different data types.
    let (x, y, z) = tup;

    println!("The value of y is: {}", y);
}


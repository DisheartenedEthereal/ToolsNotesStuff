#include <stdio.h> 
#include <stdlib.h>
int main() 
{	 
    int LuckyNumbers[] = {22,26,25};
    LuckyNumbers[0]= 2000; //lets you change a element
    printf("%d",LuckyNumbers[0]); // [X] lets u pick a specific element in array, THEY START FROM 0
	return 0; 
} 

fn main() {
    let mut x = 25; // when u add mut, the variable is changable
    println!("The value of X is {}",x);
    x = 26; // here we changed it
    // all variables are constant by default
    println!("The value of x is: {}", x);
}
